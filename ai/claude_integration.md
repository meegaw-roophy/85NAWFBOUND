Integration notes for Anthropic/Claude:

- Use the Anthropic Python client and store the API key in environment variables.
- Implement a worker/service that takes user data snapshots and generates weekly/monthly reports.
- Consider asynchronous batching and caching for cost control.
