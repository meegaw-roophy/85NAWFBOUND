# Django frontend placeholder

This directory contains a minimal Django project scaffold to serve as a placeholder UI.

Quick start:

```bash
cd frontend/django_project
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python manage.py runserver 8001
```

Decide on your final frontend (Django templates, React, Next.js) before beta; I can scaffold the chosen stack next.

The placeholder currently renders a simple dashboard page and is ready to keep as the frontend or replace with React/Next.js later.

The project repository is initialized and already mirrored to the local Git path `C:\Users\Admin\Documents\GitHub\85NAWFBOUND`.